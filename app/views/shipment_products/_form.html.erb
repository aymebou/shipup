<%= form_with(model: shipment_product, local: true) do |form| %>
  <% if shipment_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shipment_product.errors.count, "error") %> prohibited this shipment_product from being saved:</h2>

      <ul>
      <% shipment_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :shipment_id %>
    <%= form.text_field :shipment_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
